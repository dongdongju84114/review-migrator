name: Build Windows EXE

on:
  workflow_dispatch:
  push:
    paths:
      - "review_migrator/**"
      - "chrome_extensions/**"
      - "docs/staff_gui_guide.md"
      - "tools/**"
      - "tests/**"
      - "scripts/build_windows_exe.ps1"
      - "pyproject.toml"
      - ".github/workflows/build-windows-exe.yml"

jobs:
  build-windows-exe:
    runs-on: windows-2022

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[test,windows-build]"

      - name: Run tests
        run: python -m pytest

      - name: Build Windows EXE
        shell: cmd
        run: python -m PyInstaller --noconfirm --clean --onefile --windowed --name ReviewMigratorGUI --collect-submodules openpyxl --collect-submodules pandas --collect-submodules pydantic --collect-submodules dotenv --hidden-import tkinter tools/windows_gui_launcher.py

      - name: Smoke test built EXE
        shell: powershell
        run: .\dist\ReviewMigratorGUI.exe --smoke-test

      - name: Package Windows distribution
        shell: powershell
        run: |
          $packageRoot = "dist\ReviewMigratorGUI-windows"
          New-Item -ItemType Directory -Force -Path $packageRoot | Out-Null
          $extensionRoot = Join-Path $packageRoot "chrome_extensions"
          New-Item -ItemType Directory -Force -Path $extensionRoot | Out-Null
          Copy-Item -Path "dist\ReviewMigratorGUI.exe" -Destination (Join-Path $packageRoot "ReviewMigratorGUI.exe") -Force
          Copy-Item -Path "docs\staff_gui_guide.md" -Destination (Join-Path $packageRoot "staff_gui_guide.md") -Force
          Copy-Item -Path "chrome_extensions\smartstore_review_image_collector" -Destination $extensionRoot -Recurse -Force

      - name: Upload Windows package
        uses: actions/upload-artifact@v4
        with:
          name: ReviewMigratorGUI-windows
          path: dist/ReviewMigratorGUI-windows
